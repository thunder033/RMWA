machine:
  node:
    version: 5.5.0
dependencies:
  pre:
    - browserify --version || npm i browserify -g
    - grunt --version || npm i grunt grunt-cli -g
  override:
    - npm install
  post:
    - grunt
deployment:
  production:
    branch: prod
    commands:
      - sh ./deploy.sh
  staging:
    branch: master
    commands:
      - sh ./deploy.sh
general:
  artifacts:
    - "dist.tar"
notify:
  webhooks:
    - url: http://thunderlab.net/deployRMWA.php