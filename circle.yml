dependencies:
  override:
    - npm install
deployment:
  production:
    branch: prod
    commands:
      - sh ./deploy.sh
  staging:
    branch: master
    commands:
      - sh ./deploy.sh
general:
  artifacts:
    - "dist.tar"